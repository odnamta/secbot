# SecBot Hunt Registry — Diagnostic Run #1
# Created: 2026-03-22
# Purpose: First real bounty hunt — 5 carefully selected targets
# Strategy: low competition + web app depth + SecBot strengths

- name: kredivo
  platform: other
  scope_file: scopes/kredivo.txt
  profile: stealth
  schedule: weekly
  enabled: true

- name: anytask
  platform: bugcrowd
  scope_file: scopes/anytask.txt
  profile: stealth
  schedule: weekly
  enabled: true

- name: moneybird
  platform: hackerone
  scope_file: scopes/moneybird.txt
  profile: stealth
  schedule: weekly
  enabled: true

- name: openproject
  platform: other
  scope_file: scopes/openproject.txt
  profile: stealth
  schedule: weekly
  enabled: true

- name: calcom
  platform: hackerone
  scope_file: scopes/calcom.txt
  profile: stealth
  schedule: weekly
  enabled: true

# --- Added by Bounty Scout 2026-09-14 ---

# GoTo Financial: GoPay + Midtrans + MokaPos — Indonesian fintech, YesWeHack public program
# Verify automated-scanning policy on YesWeHack page before running
- name: goto-financial
  platform: yeswehack
  scope_file: scopes/goto-financial.txt
  profile: stealth
  schedule: weekly
  enabled: true

# Gojek super-app (rides/food/delivery) — YesWeHack program ONLY
# WARNING: HackerOne program explicitly bans automated scanning; this is the YesWeHack variant
# Check YesWeHack program page for automated tool policy before running
- name: gojek
  platform: yeswehack
  scope_file: scopes/gojek.txt
  profile: stealth
  schedule: weekly
  enabled: true

# Traveloka — Indonesian travel unicorn, Bugcrowd PRIVATE program
# DISABLED until Bugcrowd invite obtained (email bug@traveloka.com first)
- name: traveloka
  platform: bugcrowd
  scope_file: scopes/traveloka.txt
  profile: stealth
  schedule: weekly
  enabled: false
